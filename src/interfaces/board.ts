import { Mark } from '@/interfaces/mark'

export type Board = [
  Mark | null,
  Mark | null,
  Mark | null,
  Mark | null,
  Mark | null,
  Mark | null,
  Mark | null,
  Mark | null,
  Mark | null,
]
